[package]
name = "retrieval-package-contract"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
publish = false

[dependencies]
serde_json.workspace = true
dunce = "1"
# Use the bundler's actual resource expansion, not a second glob implementation.
# Keep this pinned with the application lockfile's Tauri version.
tauri-utils = { version = "=2.9.1", features = ["resources"] }

[dev-dependencies]
tempfile = "3.3.0"

[lints]
workspace = true
